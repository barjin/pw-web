/**
Path to the recording storage.
 */
export const savePath = `${__dirname}/../dataStore`;
/**
 * Path to the "static" folder with the React App to be served over HTTP.
 */
export const wwwPath = `${__dirname}/../www`;
